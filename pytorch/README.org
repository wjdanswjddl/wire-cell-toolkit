#+title: WCT usages of libtorch 
#+latex_header: \usepackage[margin=1in]{geometry}
#+options: ':t toc:t
#+SETUPFILE: https://fniessen.github.io/org-html-themes/org/theme-readtheorg.setup

We call this package "pytorch" but really it is C++.

It holds components that depend on libtorch.

